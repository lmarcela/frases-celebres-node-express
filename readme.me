El proyecto se descargó desde la opción recursos de https://www.udemy.com/course/la-biblia-de-nodejs/learn/lecture/17668412#overview

Para iniciarlo fue necesario correr el siguiente comando:
1) npm install --> Esto es para descargar la carpeta node_modules, la cual no se sube a git, las dependencias se guardar en package.json y es por eso que no hace falta indicarlas
2) node index --> Para correr el proyecto, el proyecto corre en el puerto definido en el archivo index.js de la carpeta config.
3) npm run dev --> Para evitar pausar el servidor cada vez que hay un cambio como ocurre con node index, esto es gracias a "dev": "nodemon index.js" del archivo package.json

Posteriormente se hicieron algunos cambios en el menu de los views y se tradujo la información a español